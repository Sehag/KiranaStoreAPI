package com.api.kiranastore.constants;

import io.github.bucket4j.Bandwidth;
import io.github.bucket4j.BucketConfiguration;
import io.github.bucket4j.Refill;
import java.time.Duration;

/*
public class BucketConstant {

    static Refill refill = Refill.intervally(1, Duration.ofSeconds(10));
    static Bandwidth limit = Bandwidth.classic(3, refill);

    public static final BucketConfiguration RATE_LIMIT_BUCKET_CONF = (BucketConfiguration.builder().addLimit(limit).build());

}

 */
